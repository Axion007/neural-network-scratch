# neural-network-scratch
A fully implemented feedforward neural network built from scratch using only NumPy. No external ML libraries! Features forward propagation, backpropagation, and solves the XOR problem.
